/**
 * Hetzner Cloud API
 * Copied from the official API documentation for the Public Hetzner Cloud.
 *
 * The version of the OpenAPI document: f6511ca-dirty
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package hcloud.models

import hcloud.models.Architecture.*
import com.github.plokhotnyuk.jsoniter_scala.macros.named

  /**
   * Image the server is based on.
   */
case class Image(
  @named("architecture") architecture: Architecture,
  /* ID of Server the Image is bound to. Only set for Images of type `backup`. */
  @named("bound_to") boundTo: Long,
  /* Point in time when the Resource was created (in ISO-8601 format). */
  @named("created") created: String,
  /* Information about the Server the Image was created from. */
  @named("created_from") createdFrom: CreatedFrom,
  /* Point in time where the Image was deleted (in ISO-8601 format). */
  @named("deleted") deleted: String,
  /* Point in time when the Image is considered to be deprecated (in ISO-8601 format). */
  @named("deprecated") deprecated: String,
  /* Description of the Image. */
  @named("description") description: String,
  /* Size of the disk contained in the Image in GB. */
  @named("disk_size") diskSize: Double,
  /* ID of the Image. */
  @named("id") id: Long,
  /* Size of the Image file in our storage in GB. For snapshot Images this is the value relevant for calculating costs for the Image. */
  @named("image_size") imageSize: Double,
  /* User-defined labels (`key/value` pairs) for the Resource. For more information, see \"Labels\".  | User-defined labels (`key/value` pairs) for the Resource.  Note that the set of Labels provided in the request will overwrite the existing one.  For more information, see \"Labels\".  */
  @named("labels") labels: Map[String, String],
  /* Unique identifier of the Image. This value is only set for system Images. */
  @named("name") name: String,
  /* Flavor of operating system contained in the Image. */
  @named("os_flavor") osFlavor: ImageEnums.OsFlavor,
  /* Operating system version. */
  @named("os_version") osVersion: String,
  @named("protection") protection: Protection,
  /* Indicates that rapid deploy of the Image is available. */
  @named("rapid_deploy") rapidDeploy: Option[Boolean] = scala.None,
  /* Whether the Image can be used or if it's still being created or unavailable. */
  @named("status") status: ImageEnums.Status,
  /* Type of the Image. */
  @named("type") `type`: ImageEnums.Type
)

object ImageEnums:
  enum OsFlavor:
    case `alma`
    case `centos`
    case `debian`
    case `fedora`
    case `opensuse`
    case `rocky`
    case `ubuntu`
    case `unknown`

  object OsFlavor:
    import com.github.plokhotnyuk.jsoniter_scala.macros.*
    import com.github.plokhotnyuk.jsoniter_scala.core.*
    given osFlavorCodec: JsonValueCodec[OsFlavor] = JsonCodecMaker.make {
      CodecMakerConfig
        .withAdtLeafClassNameMapper { x =>
          JsonCodecMaker.simpleClassName(x) match
            case "alma" => "alma"
            case "centos" => "centos"
            case "debian" => "debian"
            case "fedora" => "fedora"
            case "opensuse" => "opensuse"
            case "rocky" => "rocky"
            case "ubuntu" => "ubuntu"
            case "unknown" => "unknown"
        }
        .withDiscriminatorFieldName(scala.None)
    }
  enum Status:
    case `available`
    case `creating`
    case `unavailable`

  object Status:
    import com.github.plokhotnyuk.jsoniter_scala.macros.*
    import com.github.plokhotnyuk.jsoniter_scala.core.*
    given statusCodec: JsonValueCodec[Status] = JsonCodecMaker.make {
      CodecMakerConfig
        .withAdtLeafClassNameMapper { x =>
          JsonCodecMaker.simpleClassName(x) match
            case "available" => "available"
            case "creating" => "creating"
            case "unavailable" => "unavailable"
        }
        .withDiscriminatorFieldName(scala.None)
    }
  enum Type:
    case `app`
    case `backup`
    case `snapshot`
    case `system`

  object Type:
    import com.github.plokhotnyuk.jsoniter_scala.macros.*
    import com.github.plokhotnyuk.jsoniter_scala.core.*
    given typeCodec: JsonValueCodec[Type] = JsonCodecMaker.make {
      CodecMakerConfig
        .withAdtLeafClassNameMapper { x =>
          JsonCodecMaker.simpleClassName(x) match
            case "app" => "app"
            case "backup" => "backup"
            case "snapshot" => "snapshot"
            case "system" => "system"
        }
        .withDiscriminatorFieldName(scala.None)
    }
end ImageEnums
