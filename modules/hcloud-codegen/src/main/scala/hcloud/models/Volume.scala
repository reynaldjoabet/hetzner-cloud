/** Hetzner Cloud API Copied from the official API documentation for the Public
  * Hetzner Cloud.
  *
  * The version of the OpenAPI document: f6511ca-dirty
  *
  * NOTE: This class is auto generated by OpenAPI Generator
  * (https://openapi-generator.tech). https://openapi-generator.tech Do not edit
  * the class manually.
  */
package hcloud.models

import com.github.plokhotnyuk.jsoniter_scala.macros.named

/** A Volume is a highly-available, scalable, and SSD-based block storage for
  * Servers. Pricing for Volumes depends on the Volume size and Location, not
  * the actual used storage. Please see [Hetzner
  * Wiki](https://wiki.hetzner.de/index.php/CloudServer/en#Volumes) for more
  * details about Volumes.
  */
case class Volume(
    /* Point in time when the Resource was created (in ISO-8601 format). */
    @named("created") created: String,
    /* Filesystem of the Volume if formatted on creation, null if not formatted on creation. */
    @named("format") format: String,
    /* ID of the Volume. */
    @named("id") id: Long,
    /* User-defined labels (`key/value` pairs) for the Resource. For more information, see \"Labels\".  | User-defined labels (`key/value` pairs) for the Resource.  Note that the set of Labels provided in the request will overwrite the existing one.  For more information, see \"Labels\".  */
    @named("labels") labels: Map[String, String],
    /* Device path on the file system for the Volume. */
    @named("linux_device") linuxDevice: String,
    @named("location") location: Location,
    /* Name of the Resource. Must be unique per Project. */
    @named("name") name: String,
    @named("protection") protection: Protection,
    /* ID of the Server the Volume is attached to, null if it is not attached at all. */
    @named("server") server: Long,
    /* Size in GB of the Volume. */
    @named("size") size: Double,
    /* Status of the Volume. */
    @named("status") status: VolumeEnums.Status
)

object VolumeEnums:
  enum Status:
    case `available`
    case `creating`

  object Status:
    import com.github.plokhotnyuk.jsoniter_scala.macros.*
    import com.github.plokhotnyuk.jsoniter_scala.core.*
    given statusCodec: JsonValueCodec[Status] = JsonCodecMaker.make {
      CodecMakerConfig
        .withAdtLeafClassNameMapper { x =>
          JsonCodecMaker.simpleClassName(x) match
            case "available" => "available"
            case "creating"  => "creating"
        }
        .withDiscriminatorFieldName(scala.None)
    }
end VolumeEnums
