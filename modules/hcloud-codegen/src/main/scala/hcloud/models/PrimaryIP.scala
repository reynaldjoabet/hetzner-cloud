/**
 * Hetzner Cloud API
 * Copied from the official API documentation for the Public Hetzner Cloud.
 *
 * The version of the OpenAPI document: f6511ca-dirty
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */
package hcloud.models

import hcloud.models.IpType.*
import com.github.plokhotnyuk.jsoniter_scala.macros.named

case class PrimaryIP(
  /* ID of resource the Primary IP is assigned to.  `null` if the Primary IP is not assigned.  */
  @named("assignee_id") assigneeId: Long,
  /* Type of resource the Primary IP can get assigned to.  */
  @named("assignee_type") assigneeType: PrimaryIPEnums.AssigneeType,
  /* Auto deletion state.  If enabled the Primary IP will be deleted once the assigned resource gets deleted.  */
  @named("auto_delete") autoDelete: Boolean,
  /* Blocked state of the Primary IP. */
  @named("blocked") blocked: Boolean,
  /* Point in time when the Resource was created (in ISO-8601 format). */
  @named("created") created: String,
  @named("datacenter") datacenter: DataCenter,
  /* List of reverse DNS records. */
  @named("dns_ptr") dnsPtr: Seq[DnsPtr],
  /* ID of the Primary IP. */
  @named("id") id: Long,
  /* IP address. */
  @named("ip") ip: String,
  /* User-defined labels (`key/value` pairs) for the Resource. For more information, see \"Labels\".  | User-defined labels (`key/value` pairs) for the Resource.  Note that the set of Labels provided in the request will overwrite the existing one.  For more information, see \"Labels\".  */
  @named("labels") labels: Map[String, String],
  /* Name of the Resource. Must be unique per Project. */
  @named("name") name: String,
  @named("protection") protection: Protection,
  @named("type") `type`: IpType
)

object PrimaryIPEnums:
  enum AssigneeType:
    case `server`

  object AssigneeType:
    import com.github.plokhotnyuk.jsoniter_scala.macros.*
    import com.github.plokhotnyuk.jsoniter_scala.core.*
    given assigneeTypeCodec: JsonValueCodec[AssigneeType] = JsonCodecMaker.make {
      CodecMakerConfig
        .withAdtLeafClassNameMapper { x =>
          JsonCodecMaker.simpleClassName(x) match
            case "server" => "server"
        }
        .withDiscriminatorFieldName(scala.None)
    }
end PrimaryIPEnums
